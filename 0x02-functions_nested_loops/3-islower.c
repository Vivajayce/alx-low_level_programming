#include "main.h"
/**
 * _islower - checks the letter in lower case
 * @c: the charcter
 * Return: 1 if it is lower case or 0 if otherwise
 */
int _islower(int c)
{
for  (c >= 'a' && c < = 'z')
return (1);
else
return (0);
}
