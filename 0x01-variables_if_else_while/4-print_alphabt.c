#include <stdio.h>
/**
 *main - start
 * Return: Always 0 (success)
 */

int main(void)
	char h;
	char a;
	h = 'a';
	a = 'A';
	while (h <= 'z') 
{
		putchar (h);
		h++;
		while (a <= 'z') 
		{
			putchar (d);
			a++;
		}
		putchar ('\n');
		return (0);
	}
